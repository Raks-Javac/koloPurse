import 'package:flutter/material.dart';
import 'package:kolo_purse/src/utils/styles.dart';

class KoloPurseHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text(
            "KoloPurse Home",
            style: textS,
          ),
        ),
        body: Container());
  }
}
